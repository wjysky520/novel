spring:
  application:
    name: novel-eureka
  cloud:
    nacos:
      config:
        # 配置中心的地址
        server-addr: nacos.wjysky.com
        namespace: e091fe26-2f07-420d-a8da-d277efa0c1fd
        ext-config[0]:
          data-id: novel-eureka-${spring.profiles.active}.yaml
          group: ${spring.profiles.active}
          refresh: true