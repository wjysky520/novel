spring:
  application:
    name: novel-file
  cloud:
    nacos:
      config:
        # 配置中心的地址
        server-addr: nacos.wjysky.com
        namespace: e091fe26-2f07-420d-a8da-d277efa0c1fd
        ext-config[0]:
          data-id: novel-file-${spring.profiles.active}.yaml
          group: ${spring.profiles.active}
          refresh: true
        ext-config[1]:
          data-id: minio.yaml
          group: DEFAULT_GROUP
          refresh: true

logging:
  config: http://${spring.cloud.nacos.config.server-addr}/nacos/v1/cs/configs?group=${spring.profiles.active}&tenant=e091fe26-2f07-420d-a8da-d277efa0c1fd&username=nacos&password=nacos&dataId=logback-${spring.profiles.active}.xml