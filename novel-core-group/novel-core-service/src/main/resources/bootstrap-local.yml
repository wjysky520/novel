server:
  port: 9911
  name: novel-core
  servlet:
    context-path: /novel-core

eureka:
  instance:
    instance-id: ${server.name}
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/

log:
  path: D:/logs/novel-core