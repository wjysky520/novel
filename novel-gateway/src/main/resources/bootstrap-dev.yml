spring:
  application:
    name: novel-gateway
  cloud:
    nacos:
      config:
        # 配置中心的地址
        server-addr: nacos.wjysky.com
        namespace: e091fe26-2f07-420d-a8da-d277efa0c1fd
        ext-config[0]:
          file-extension: yaml
          group: ${spring.profiles.active}
          data-id: novel-gateway-${spring.profiles.active}.yaml
          refresh: true